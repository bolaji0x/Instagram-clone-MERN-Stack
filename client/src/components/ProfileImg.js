import React from 'react'
import logo from '../assets/images/profilepics.png'

const ProfileImg = ({className}) => {
  return <img src={logo} alt="instagram-logo" className={className} />
}

export default ProfileImg